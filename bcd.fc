#fc 3
@(bank: -1);

@include("macro.asm");
@include("bcd.asm");

const BCD_MAX_SIZE = 8;
var bcd_buf:[BCD_MAX_SIZE]u8;

public function bcd_set(to:*u8, size:u8, n:u16):*u8;

public function bcd_add(to:*u8, size:u8, n:*u8):*u8;
