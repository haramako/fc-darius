#fc 2
options(bank: -1);

include("macro.asm");
include("bcd.asm");

const BCD_MAX_SIZE = 8;
var bcd_buf:[BCD_MAX_SIZE]int;

public function bcd_set(to:*int, size:int, n:int16):*int;

public function bcd_add(to:*int, size:int, n:*int):*int;
